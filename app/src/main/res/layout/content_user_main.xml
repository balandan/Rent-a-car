<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".user.UserMainActivity"
    tools:showIn="@layout/app_bar_user_main">

    <EditText
        android:id="@+id/car_searchBar"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:text=""
        android:cursorVisible="false"
        android:textSize="17sp" />

    <ImageView
        android:id="@+id/car_searchIcon"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_alignEnd = "@id/car_searchBar"
        android:src="@drawable/ic_search"
        android:layout_alignRight="@id/car_searchBar" />
    
    <ListView
        android:layout_marginTop="50dp"
        android:id="@+id/cars_ListView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbars="vertical">


    </ListView>

</RelativeLayout>